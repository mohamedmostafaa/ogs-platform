/**
 * Tailwind v4 PostCSS config — required for `pnpm dev` and `next build`
 * to process the global stylesheet imported from `@ogs/ui`.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
